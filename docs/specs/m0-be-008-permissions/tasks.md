# M0-BE-008 Permissions Tasks

1. Add permission metadata, permission extension, endpoint filter, permission checker, and constants in `WeCms.Modules.System/Permissions`.
2. Add SqlSugar-backed permission repository in `WeCms.Persistence/Modules/System/Permissions`.
3. Register permission services and map `secure-ping`.
4. Add metadata scan tests and permission filter tests.
5. Add MySQL integration tests for permission checker.
6. Run focused tests, backend gate, and task audit.
