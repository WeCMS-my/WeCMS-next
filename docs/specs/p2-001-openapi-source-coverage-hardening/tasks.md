# P2-001 OpenAPI Source Coverage Hardening Tasks

1. Add a change-specific spec trio for OpenAPI source coverage hardening.
2. Update OpenAPI unit tests to discover endpoint mapping files automatically under `WeCms.Modules.System`.
3. Keep the existing exported-contract assertions while removing the hardcoded three-file coverage limitation.
4. Run focused OpenAPI tests, then rerun the backend quality gate and audit before closing the task.
