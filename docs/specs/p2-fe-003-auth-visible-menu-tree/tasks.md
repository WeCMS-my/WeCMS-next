# P2-FE-003 Tasks

- [ ] Add spec trio before implementation.
- [ ] Add failing tests for auth responses and frontend fallback when user lacks `sys:menu:tree`.
- [ ] Add backend repository support to load a user's visible menu tree.
- [ ] Return `MenuTreeDto[]` from `LoginResponse` and `AuthMeResponse`.
- [ ] Update frontend stores and router guards to use auth-returned menu trees as fallback effective menus.
- [ ] Update OpenAPI artifact and frontend generated types.
- [ ] Run backend build, test, and publish.
- [ ] Run frontend typecheck, lint, and build.
- [ ] Run current task audit and final scope audit.
