# Checklist

- [x] User enable/disable/reset/assign clients return `ApiResult<unknown>`.
- [x] Role enable/disable/assign clients return `ApiResult<unknown>`.
- [x] Menu, permission, department, and post enable/disable clients return `ApiResult<unknown>`.
- [x] Create/update clients still use their mutation response DTOs.
- [x] Frontend typecheck/build and frontend quality gate pass.
