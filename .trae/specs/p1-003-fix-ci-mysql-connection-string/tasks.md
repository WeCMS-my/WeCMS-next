# Tasks

- [x] Task 1: Fix CI connection string environment variable name
  - [x] Change `WeCMS__ConnectionStrings__Default` to `ConnectionStrings__Default` in `.github/workflows/backend-quality-gate.yml` line 73

# Task Dependencies
- No dependencies — single isolated CI config fix.
